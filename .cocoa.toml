[changelog.sections]
breaking = "breaking changes"
feat = "new features"
fix = "bug fixes"
perf = "performance"
docs = "documentation"
test = "tests"
build = "build system"
ci = "continuous integration"

[version]
commit_version_files = ["Cargo.toml"]
